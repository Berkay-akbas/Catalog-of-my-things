CREATE TABLE label(id INT GENERATED BY DEFAULT AS IDENTITY, title VARCHAR, color VARCHAR, PRIMARY KEY (id))

CREATE TABLE books(id INT GENERATED BY DEFAULT AS IDENTITY, publisher VARCHAR, cover_state VARCHAR, publish_date DATE, archived BOOLEAN, PRIMARY KEY (id), label_id INT, FOREIGN KEY (label_id) REFERENCES label(id));


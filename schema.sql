CREATE TABLE item(id INT GENERATED BY DEFAULT AS IDENTITY, publish_date DATE, archived BOOLEAN, books_id INT, PRIMARY KEY (id), FOREIGN KEY (books_id) REFERENCES books(id));

CREATE TABLE books(id INT GENERATED BY DEFAULT AS IDENTITY, publisher VARCHAR, cover_state VARCHAR);